#pragma once

#include<iostream>
using namespace std;

enum FRIEND_REQUEST_ENUM {
    ACCEPTED,
    REJECTED,
    PENDING
};

extern int profileId;
extern int postId;
extern int likeId;
extern int commentId;